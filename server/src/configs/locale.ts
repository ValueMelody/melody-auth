export enum Error {
  NoApp = 'No app found',
  WrongRedirectUri = 'Wrong redirect uri provided',
}

export enum AuthorizePage {
  EmailLabel = 'Email',
  PasswordLabel = 'Password',
  SubmitBtn = 'Login',
}
